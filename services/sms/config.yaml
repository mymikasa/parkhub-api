server:
  grpc_port: 50053
  metrics_port: 9093
  http_health_port: 8082

database:
  host: localhost
  port: 3306
  user: parkhub
  password: parkhub
  dbname: parkhub_identity

redis:
  addr: localhost:6379

telemetry:
  service_name: parkhub-sms
  environment: development
  log:
    level: info
    format: json
  trace:
    endpoint: localhost:4317
    insecure: true
    sampling_ratio: 1.0
  metrics:
    path: /metrics
