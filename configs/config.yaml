server:
  grpc_port: 50051
  metrics_port: 9090

database:
  host: localhost
  port: 3306
  user: parkhub
  password: parkhub
  dbname: parkhub_identity

telemetry:
  service_name: parkhub-monolith
  environment: development
  log:
    level: info
    format: text
  trace:
    endpoint: localhost:4317
    insecure: true
    sampling_ratio: 1.0
  metrics:
    path: /metrics
